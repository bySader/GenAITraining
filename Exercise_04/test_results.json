[
  {
    "id": 1,
    "scope": "in",
    "question": "What are Python decorators and how do I use them?",
    "answer": "Python decorators are a powerful and expressive feature in Python. A decorator is a function that takes another function as input and extends or alters its behavior without modifying the original function's source code.\n\n**Basic Decorator Syntax**\n\nA decorator is applied using the `@` symbol before a function definition. Here's an example:\n\n```python\ndef my_decorator(func):\n    def wrapper(*args, **kwargs):\n        print(\"Before the function runs\")\n        result = func(*args, **kwargs)\n        print(\"After the function runs\")\n        return result\n    return wrapper\n\n@my_decorator\ndef say_hello():\n    print(\"Hello!\")\n\nsay_hello()\n# Output:\n# Before the function runs\n# Hello!\n# After the function runs\n```\n\nIn this example, `my_decorator` is a function that takes `say_hello` as input and returns a new function `wrapper`. When `say_hello` is called, it actually calls the `wrapper` function, which prints \"Before the function runs\", calls `say_hello` (which prints \"Hello!\"), and then prints \"After the function runs\".\n\n**Using `functools.wraps`**\n\nWhen using decorators, it's a good practice to use `functools.wraps` to preserve the original function's metadata (name, docstring, etc.). Here's an example:\n\n```python\nimport functools\n\ndef my_decorator(func):\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        return func(*args, **kwargs)\n    return wrapper\n\n@my_decorator\ndef say_hello():\n    \"\"\"Prints a greeting\"\"\"\n    print(\"Hello!\")\n\nprint(say_hello.__name__)  # Output: say_hello\nprint(say_hello.__doc__)   # Output: Prints a greeting\n```\n\n**Decorators with Arguments**\n\nYou can create decorators that accept arguments by adding another layer of nesting. Here's an example:\n\n```python\ndef repeat(n):\n    def decorator(func):\n        @functools.wraps(func)\n        def wrapper(*args, **kwargs):\n            for _ in range(n):\n                func(*args, **kwargs)\n        return wrapper\n    return decorator\n\n@repeat(3)\ndef greet():\n    print(\"Hello!\")\n\ngreet()\n# Output:\n# Hello!\n# Hello!\n# Hello!\n```\n\nIn this example, `repeat` is a function that takes an argument `n` and returns a decorator that repeats the function `n` times.",
    "sources": [
      "python_decorators.txt",
      "kanban_guide.docx",
      "python_intro.md"
    ],
    "in_scope_detected": true
  },
  {
    "id": 2,
    "scope": "in",
    "question": "Explain the four pillars of object-oriented programming.",
    "answer": "The four pillars of object-oriented programming (OOP) are:\n\n### 1. Encapsulation\n\nEncapsulation is the concept of bundling data and methods that operate on that data into a single unit, called a class. This helps to hide the internal details of an object from the outside world and provides a way to control access to the data. Encapsulation promotes data hiding, which is essential for maintaining the integrity of an object's state.\n\n```python\nclass BankAccount:\n    def __init__(self, balance: float):\n        self.__balance = balance  # private attribute\n\n    def deposit(self, amount: float):\n        self.__balance += amount\n\n    def get_balance(self) -> float:\n        return self.__balance\n```\n\n### 2. Inheritance\n\nInheritance is a mechanism that allows one class to inherit the properties and behavior of another class. The child class inherits all the attributes and methods of the parent class and can also add new attributes and methods or override the ones inherited from the parent class.\n\n```python\nclass Animal:\n    def speak(self):\n        return \"Some sound\"\n\nclass Dog(Animal):\n    def speak(self):\n        return \"Woof!\"\n\nclass Cat(Animal):\n    def speak(self):\n        return \"Meow!\"\n```\n\n### 3. Polymorphism\n\nPolymorphism is the ability of an object to take on multiple forms. This can be achieved through method overriding or method overloading. Method overriding occurs when a subclass provides a different implementation of a method that is already defined in its superclass. Method overloading occurs when multiple methods with the same name can be defined, but with different parameter lists.\n\n```python\nclass Shape:\n    def area(self):\n        pass\n\nclass Circle(Shape):\n    def __init__(self, radius: float):\n        self.radius = radius\n\n    def area(self) -> float:\n        return 3.14159 * self.radius ** 2\n```\n\n### 4. Abstraction\n\nAbstraction is the concept of showing only the essential features of an object while hiding its internal details. This helps to reduce complexity and improve modularity. Abstraction is achieved through the use of abstract classes and interfaces.\n\n```python\nfrom abc import ABC, abstractmethod\n\nclass Shape(ABC):\n    @abstractmethod\n    def area(self) -> float:\n        pass\n```\n\nThese four pillars of OOP - encapsulation, inheritance, polymorphism, and abstraction - work together to provide a robust and flexible way of designing and implementing software systems.",
    "sources": [
      "python_oop.md",
      "python_intro.md",
      "database_design.pdf"
    ],
    "in_scope_detected": true
  },
  {
    "id": 3,
    "scope": "in",
    "question": "What is the difference between Git merge and Git rebase?",
    "answer": "I don't know, that is not part of my context.",
    "sources": [
      "ci_cd_pipeline.json",
      "git_workflow.txt"
    ],
    "in_scope_detected": true
  },
  {
    "id": 4,
    "scope": "out",
    "question": "What is the capital of France?",
    "answer": "I don't know, that is not part of my context.",
    "sources": [
      "ci_cd_pipeline.json",
      "ml_fundamentals.pdf",
      "ml_fundamentals.txt"
    ],
    "in_scope_detected": true
  },
  {
    "id": 5,
    "scope": "out",
    "question": "Who won the FIFA World Cup in 2022?",
    "answer": "I'm sorry, that question is outside the scope of my knowledge base. I can only answer questions about Programming, DevOps, Project Management, Machine Learning, and Databases.",
    "sources": [],
    "in_scope_detected": false
  }
]